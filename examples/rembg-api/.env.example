# rembg-api 配置模板：复制为 .env 或直接 export

# 监听地址 / 端口
REMBG_API_HOST=0.0.0.0
REMBG_API_PORT=8000

# rembg 模型名（首次使用会从 GitHub 下载，约 176MB）
# 可选：u2net / u2netp / isnet-general-use / isnet-anime / silueta
REMBG_MODEL=isnet-general-use

# 上传大小上限（MB）
REMBG_MAX_UPLOAD_MB=20
